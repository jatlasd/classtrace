export default function SettingsPage() {
  return (
    <div className="mx-auto max-w-2xl px-4 py-8 sm:px-6">
      <section className="rounded-card border border-border bg-card p-6 shadow-paper">
        <p className="text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">
          Settings
        </p>
        <h1 className="font-display mt-2 text-2xl font-semibold text-foreground">
          Settings coming soon
        </h1>
        <p className="mt-2 text-sm leading-relaxed text-muted-foreground">
          Account and workspace settings will be added in a later build unit.
        </p>
      </section>
    </div>
  );
}
